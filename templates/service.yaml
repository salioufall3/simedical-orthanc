apiVersion: v1
kind: Service
metadata:
  name: {{ include "simedical-orthanc.fullname" . }}
  labels:
    {{- include "simedical-orthanc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "simedical-orthanc.selectorLabels" . | nindent 4 }}
