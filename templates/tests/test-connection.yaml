apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "simedical-orthanc.fullname" . }}-test-connection"
  labels:
    {{- include "simedical-orthanc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "simedical-orthanc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
